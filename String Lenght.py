# the program will calculate the lenght of the string



def str_len(string):
    string = input("Enter a string here: " )
    length = len(string)
    print("The length of the string is " + str(length) + " characters")
    return

